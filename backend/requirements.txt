# ============================================
# QQ智能助手 - Python 依赖 (Python 3.12+ / CUDA 13.0+)
# 安装: pip install -r requirements.txt
# GPU加速: pip install torch faiss-gpu vllm --index-url https://download.pytorch.org/whl/cu130
# ============================================

# Web框架 (Python 3.12 兼容)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.10.0,<3.0
starlette>=0.40.0,<2.0

# NoneBot2 (Python 3.12 兼容)
nonebot2>=2.5.0,<3.0
nonebot-adapter-onebot>=2.4.6,<3.0

# 认证
pyjwt>=2.13.0,<3.0
bcrypt>=4.2.0,<6.0

# 数据库 (PostgreSQL可选)
sqlalchemy[asyncio]>=2.0.36,<3.0
alembic>=1.14.0,<2.0
asyncpg>=0.30.0,<1.0

# HTTP客户端
httpx>=0.28.0,<1.0
aiohttp>=3.11.0,<4.0

# 工具库
python-dotenv>=1.0.0
python-multipart>=0.0.20
python-json-logger>=2.0.7
python-dateutil>=2.9.0
pytz>=2024.2
psutil>=6.1.0

# ── AI/ML 核心 (CUDA 13.0 ─ 服务器 RTX 3090 驱动 595.58.03 / CUDA 13.2) ──
# PyTorch 2.11+ 默认 CUDA 13: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
torch>=2.11.0,<3.0
transformers>=5.0.0,<6.0
peft>=0.14.0,<1.0
bitsandbytes>=0.45.0,<1.0
datasets>=5.0.0,<6.0
accelerate>=1.4.0,<2.0
sentencepiece>=0.2.0,<1.0
tqdm>=4.67.0,<5.0

# vLLM 推理 (0.20+ 原生支持 CUDA 13 + transformers v5)
vllm>=0.22.0,<1.0

# 模型下载
huggingface-hub>=0.27.0,<2.0
requests>=2.32.0,<3.0

# 向量数据库和RAG
faiss-cpu>=1.9.0,<2.0            # CPU版开发用，生产改 faiss-gpu
sentence-transformers>=5.0.0,<6.0
numpy>=2.0.0,<3.0
scipy>=1.14.0,<2.0
scikit-learn>=1.6.0,<2.0
jieba>=0.42.1,<1.0

# Redis缓存
redis>=5.2.0,<9.0
hiredis>=3.1.0,<4.0

# 加密 (AES-256-GCM)
cryptography>=44.0.0,<50.0
