# ══════════════════════════════════════════════════
# QQ 智能助手 - 均衡推荐版本锁定
# Python 3.11 | CUDA 12.1 | PyTorch 2.4.1
# ══════════════════════════════════════════════════

# ── Web 框架 ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# ── NoneBot2 ──
nonebot2==2.4.1
nonebot-adapter-onebot==2.4.3

# ── 认证 ──
PyJWT==2.10.1
bcrypt==4.2.1

# ── 数据库 ORM + 迁移 ──
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0

# ── 数据库加密 ──
cryptography==44.0.0

# ── HTTP 客户端 ──
httpx==0.28.1
aiohttp==3.11.11

# ── 工具库 ──
python-dotenv==1.0.1
python-multipart==0.0.20
python-json-logger==2.0.7

# ── 日期时间 ──
python-dateutil==2.9.0
pytz==2024.2

# ── 系统监控 ──
psutil==6.1.1

# ── PyTorch + 推理生态（CUDA 12.1） ──
# ⚠️ 安装时请先执行：
#   pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 torchaudio==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# ⚠️ 然后再执行：pip install -r requirements.txt
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
transformers==4.46.1
peft==0.14.0
trl==0.12.2
bitsandbytes==0.44.1
datasets==3.2.0
accelerate==1.2.1
sentencepiece==0.2.0
tqdm==4.67.1

# ── vLLM 推理引擎 ──
# ⚠️ 与 torch 2.4.1 + CUDA 12.1 兼容
vllm==0.7.2

# ── 模型下载 ──
huggingface-hub==0.27.0
requests==2.32.3

# ── 向量数据库和 RAG ──
faiss-cpu==1.9.0.post1
sentence-transformers==3.3.1
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.6.1
jieba==0.42.1

# ── Redis 缓存 ──
redis==5.2.1
hiredis==3.1.0
