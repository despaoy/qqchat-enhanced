# ============================================
# QQ智能助手 - Python 依赖
# 目标环境: Python 3.12+ / CUDA 13.0+ (RTX 3090 驱动 595+)
# ============================================
#
# ⚠️ 重要安装顺序（GPU 服务器）:
#
#   # 1. 先装 PyTorch CUDA 13.0 版本（vllm 0.22.1 严格依赖 torch==2.11.0）
#   pip install torch==2.11.0 torchvision==0.26.0 torchaudio==2.11.0 \
#       --index-url https://download.pytorch.org/whl/cu130
#
#   # 2. 再装 vLLM（必须 0.22.1，勿用 0.23.0+，会有 pip 元数据解析 bug）
#   pip install vllm==0.22.1
#
#   # 3. 最后装其余依赖（torch/vllm 会被跳过）
#   pip install -r requirements.txt
#
# CI/Docker 环境（无 GPU）:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#   grep -v -E "^(torch|torchvision|torchaudio|vllm)" requirements.txt > requirements-ci.txt
#   pip install -r requirements-ci.txt
# ============================================

# ── AI/ML 核心（CUDA 13.0 专用，须按上述顺序单独安装）──
# vllm 0.22.1: 原生支持 CUDA 13 + transformers v5；0.23.0 有 pip 元数据 bug，勿用
# torch 2.11.0: vllm 0.22.1 的硬性依赖，cu130 构建来自 download.pytorch.org/whl/cu130
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
vllm==0.22.1

# Transformers v5（2026.01 稳定大版本，v4 已过时）
transformers>=5.0.0,<6.0

# 训练框架
peft>=0.14.0,<1.0
bitsandbytes>=0.45.0,<1.0
datasets>=5.0.0,<6.0
accelerate>=1.4.0,<2.0
sentencepiece>=0.2.0,<1.0
tqdm>=4.67.0,<5.0

# Web 框架
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.10.0,<3.0
starlette>=0.40.0,<2.0

# NoneBot2（QQ 机器人）
nonebot2>=2.5.0,<3.0
nonebot-adapter-onebot>=2.4.6,<3.0

# 认证与加密
pyjwt>=2.13.0,<3.0
bcrypt>=4.2.0,<6.0
cryptography>=44.0.0,<50.0      # AES-256-GCM 敏感字段加密

# 数据库
sqlalchemy[asyncio]>=2.0.36,<3.0
alembic>=1.14.0,<2.0
asyncpg>=0.30.0,<1.0           # PostgreSQL 异步驱动（可选）

# HTTP 客户端
httpx>=0.28.0,<1.0
aiohttp>=3.11.0,<4.0

# 工具库
python-dotenv>=1.0.0
python-multipart>=0.0.20
python-json-logger>=2.0.7
python-dateutil>=2.9.0
pytz>=2024.2
psutil>=6.1.0

# 模型下载
huggingface-hub>=0.27.0,<2.0
requests>=2.32.0,<3.0

# 向量数据库与 RAG
faiss-cpu>=1.9.0,<2.0           # CPU 版开发用，生产可换 faiss-gpu
sentence-transformers>=5.0.0,<6.0
numpy>=2.0.0,<3.0
scipy>=1.14.0,<2.0
scikit-learn>=1.6.0,<2.0
jieba>=0.42.1,<1.0              # 中文分词

# Redis 缓存（可选，未配置时后端自动降级为数据库直连）
redis>=5.2.0,<9.0
hiredis>=3.1.0,<4.0
