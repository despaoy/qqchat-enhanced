# ============================================
# QQ智能助手 - Python 依赖 (Python 3.12+ / CUDA 12.6+)
# 安装: pip install -r requirements.txt
# GPU加速: pip install torch faiss-gpu vllm --index-url https://download.pytorch.org/whl/cu126
# ============================================

# Web框架 (Python 3.12 兼容)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
starlette>=0.40.0

# NoneBot2 (Python 3.12 兼容)
nonebot2>=2.5.0
nonebot-adapter-onebot>=2.4.6

# 认证
pyjwt>=2.13.0
bcrypt>=4.2.0

# 数据库 (PostgreSQL可选)
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0

# HTTP客户端
httpx>=0.28.0
aiohttp>=3.11.0

# 工具库
python-dotenv>=1.0.0
python-multipart>=0.0.20
python-json-logger>=2.0.7
python-dateutil>=2.9.0
pytz>=2024.2
psutil>=6.1.0

# ── AI/ML 核心 (CUDA 12.6 ─ 若用 CUDA 13 请改用 nightly) ──
# PyTorch: 标准安装 pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu126
# CUDA 13: pip install torch --index-url https://download.pytorch.org/whl/nightly/cu130
torch>=2.6.0
transformers>=4.48.0
peft>=0.14.0
bitsandbytes>=0.45.0
datasets>=3.2.0
accelerate>=1.4.0
sentencepiece>=0.2.0
tqdm>=4.67.0

# vLLM 推理 (pip install vllm)
# vllm>=0.8.0

# 模型下载
huggingface-hub>=0.27.0
requests>=2.32.0

# 向量数据库和RAG
faiss-cpu>=1.9.0            # CPU版开发用，生产改 faiss-gpu
sentence-transformers>=4.0.0
numpy>=1.26.0
scipy>=1.14.0
scikit-learn>=1.6.0
jieba>=0.42.1

# Redis缓存
redis>=5.2.0
hiredis>=3.1.0

# 加密 (AES-256-GCM)
cryptography>=44.0.0
